import { Grid } from '@mui/material';
import React from 'react';
import Sidebar from '../components/sidebar/sidebar';

const Home = () => {
  return (
    <Grid>
      <Sidebar />
    </Grid>
  );
};

export default Home;
